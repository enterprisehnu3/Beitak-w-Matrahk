# منصة بيتك و مطرحك (Beitak W Matrak)

منصة سكن مشترك للمصريين - طلاب وموظفين.

## المتطلبات
- Python 3.8+
- MySQL Database (أو SQLite للتجربة)

## طريقة التشغيل

1. **تثبيت المكتبات المطلوبة:**
   ```bash
   pip install -r requirements.txt
   ```

2. **إعداد قاعدة البيانات:**
   - تأكد من تشغيل MySQL Server.
   - قم بإنشاء قاعدة بيانات باسم `beitak_db`.
   - قم بتعديل ملف `config.py` إذا كان لديك كلمة مرور لـ root أو اسم مستخدم مختلف.
   
   *ملاحظة: إذا أردت التجربة السريعة بدون MySQL، يمكنك تغيير `SQLALCHEMY_DATABASE_URI` في `config.py` إلى `sqlite:///site.db`.*

3. **تشغيل التطبيق:**
   ```bash
   python app.py
   ```
   سيقوم التطبيق بإنشاء الجداول وإضافة بيانات تجريبية (مستخدمين وإعلانات) عند التشغيل لأول مرة.

4. **الدخول:**
   افتح المتصفح على `http://127.0.0.1:5000`

## 👥 بيانات الدخول للحسابات التجريبية (للاختبار والتقييم)

يمكنكم استخدام الحسابات التالية لتجربة المنصة واستعراض الصلاحيات المختلفة واللوحات الخاصة بكل نوع مستخدم (أدمن، مالك عقار، طالب/مستأجر):

| الاسم الكامل | اسم المستخدم | البريد الإلكتروني | نوع الحساب (الدور) | كلمة المرور |
| :--- | :--- | :--- | :--- | :--- |
| **ابراهيم محمد** | `ebrahim_admin` | `ebrahimadmin@beitak.com` | 🛠️ مدير النظام (Admin) | `admin123` |
| **ايهاب احمد** | `ehab_ahmed` | `ehabahmed@beitak.com` | 🛠️ مدير النظام (Admin) | `admin123` |
| **أبو بكر خالد** | `abubakr_khaled` | `abubakrkhaled@beitak.com` | 🏠 مالك عقار (Homeowner) | `1010` |
| **عبده سالم** | `abdo_salem` | `abdosalem@beitak.com` | 🏠 مالك عقار (Homeowner) | `1010` |
| **احمد محمود** | `ahmed_Mahmoud` | `ahmedmahmoud@beitak.com` | 🏠 مالك عقار (Homeowner) | `1010` |
| **أحمد عبدالناصر** | `ahmed_saleh` | `ahmedsaleh@beitak.com` | 🎓 طالب / مستأجر (Student) | `1010` |
| **عمرو محمد** | `amr_mohamed` | `shtabwbkr@gmail.com` | 🎓 طالب / مستأجر (Student) | `1010` |
| **سعيد محمد** | `saeed_mohamed` | `saeedmohamed@beitak.com` | 🎓 طالب / مستأجر (Student) | `1010` |
| **محمود احمد** | `mahmoud_ahmed` | `mahmoudahmed@beitak.com` | 🎓 طالب / مستأجر (Student) | `1010` |

> [!NOTE]
> تم تفعيل وتشفير كلمات المرور لجميع الحسابات السابقة في قاعدة البيانات. تم تعزيز حماية معاملات المنصة وعمولتها عبر حجب وسائل التواصل الخارجية المباشرة (مثل البريد الشخصي والهاتف) من فواتير الدفع، وتوجيه المستخدمين للتواصل حصرياً عبر **نظام المحادثة الفورية الداخلي** باستخدام اسم المستخدم (`@username`).

## المميزات
- تصميم متجاوب (Responsive) وداعم للغة العربية (RTL).
- نظام تسجيل دخول وصلاحيات (طالب، مالك، أدمن).
- بحث وفلترة (السعر، المدينة، النوع).
- نظام توافق ذكي (Compatibility Match).
- لوحة تحكم لكل نوع مستخدم.
